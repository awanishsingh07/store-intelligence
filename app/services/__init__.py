# Business logic services — to be implemented:
#   ingestion.py   — dedup + persist events
#   metrics.py     — compute store metrics
#   funnel.py      — session-level funnel aggregation
#   heatmap.py     — zone frequency + dwell normalisation
#   anomalies.py   — anomaly detection rules
